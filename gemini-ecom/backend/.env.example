# Server Configuration
PORT=5001
NODE_ENV=development

# MongoDB Connection
MONGO_URI=mongodb://mongo:27017/bd-sourcing

# JWT Secret
JWT_SECRET=a-very-strong-and-long-secret-key-for-jwt

# Admin User
# This is a simple way to protect admin routes.
# The admin middleware will check if the user's ID matches this value.
ADMIN_USER_ID=replace-with-an-actual-user-id-from-db-after-seeding

# CORS Origin
# The frontend URL that is allowed to make requests to this backend.
CORS_ORIGIN=http://localhost:3000
